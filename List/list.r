list <- list(1, 2, 3)

typeof(list)

str(list)

# accessing list element

list[c(1)]

list[c(1:3)]

# excluding list element

list[-1]

# accessing list elements using logical vector

list[c(T, T, T)]
